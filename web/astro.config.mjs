// @ts-check
import { defineConfig } from "astro/config";

export default defineConfig({
  site: "https://garychen-soc.github.io",
  base: "/tw-card-registration-radar",
  build: { format: "file" },
});
